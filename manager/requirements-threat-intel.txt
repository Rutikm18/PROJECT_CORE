# manager/requirements-threat-intel.txt — threat-intel service only
# Subset of requirements.txt: threat_intel_service.py imports fastapi,
# asyncpg (via indexer/pg_pool), and aiohttp (via feeds/nvd). Nothing else.

fastapi>=0.111.0
uvicorn[standard]>=0.29.0
asyncpg>=0.29.0
aiohttp>=3.9.0
