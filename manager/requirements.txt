# manager/requirements.txt — mac_intel manager dependencies
# Install: pip install -r manager/requirements.txt

# Web framework + ASGI server
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Async Postgres
asyncpg>=0.29.0

# Async SQLite (TelemetryIndex in index.py — the raw-payload/Deep Analysis
# index is a separate store from manager.db/intel.db and was not part of
# the Postgres migration)
aiosqlite>=0.20.0

# Crypto — AES-256-GCM, HKDF (shared with agent)
cryptography>=42.0.0

# Jinja2 templating (for dashboard HTML rendering)
jinja2>=3.1.0

# YAML rule-pack loader
PyYAML>=6.0.0

# For serving static files
python-multipart>=0.0.9

# Async HTTP client (threat feed fetching)
aiohttp>=3.9.0

# RabbitMQ async client (Phase 2 queue pipeline)
aio-pika>=9.0.0

# AI analysis engine (Claude API)
anthropic>=0.40.0

# Stateful post-detection investigations with durable human approval checkpoints
langgraph>=1.1.0,<2.0.0
langgraph-checkpoint-postgres>=3.0.0,<4.0.0
psycopg[binary,pool]>=3.2.0,<4.0.0

# Async SMTP for email notifications
aiosmtplib>=3.0.0
