# =============================================================================
#  docker-compose.prod.yml — DEPRECATED
#
#  This file is no longer needed.
#  Both IP-only and domain (Let's Encrypt) modes are now handled by the
#  main docker-compose.yml via a Caddyfile generated by env.sh.
#
#  Setup:
#    bash env.sh          ← one-time interactive setup
#    docker compose up -d ← start everything
# =============================================================================
